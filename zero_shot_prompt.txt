function convertToSnakeCase(text) {
    return text
        .trim()
        .replace(/([a-z])([A-Z])/g, '$1_$2')
        .replace(/[\s\-\.]+/g, '_')
        .replace(/[^\w_]/g, '')
        .toLowerCase();
}